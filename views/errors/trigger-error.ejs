
        <% if (title) { %>
            <h1><%= title %></h1>
        <% } else { %>
            <h1>Error</h1>
        <% } %>

        <p><%= message || "Something went wrong. Please try again later." %></p>
        
        <a href="/">Return to Home</a>
